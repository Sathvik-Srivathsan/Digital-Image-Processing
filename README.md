# OpenCV Image Processing Notebook

A personal OpenCV notebook with a clean set of image processing experiments.  
It covers transforms, histogram work, filtering, noise, restoration, and frequency domain methods in one place.

## What is inside

- Image transforms
- Gray level transformations
- Histogram processing
- Spatial filtering
- Smoothing filters
- Sharpening filters
- Noise generation and analysis
- Restoration with spatial filters
- Periodic noise reduction in the frequency domain
- Degradation function estimation
- Inverse filtering

## Tech stack

- Python
- OpenCV
- NumPy
- Matplotlib
- SciPy
- PyWavelets

## How to use

1. Open the notebook in Jupyter or VS Code.
2. Install the required packages.
3. Run the cells in order.

Example install:

```bash
pip install opencv-python numpy matplotlib scipy PyWavelets
```

## Project structure

- `DIP_Assignment.ipynb` - main notebook with all experiments
- `README.md` - project overview

## Notes

- The notebook uses a sample grayscale image for the experiments.
- Each section is kept separate so the effect of each operation is easy to compare.

## GitHub description

OpenCV notebook covering transforms, histogram processing, filtering, noise, restoration, and frequency domain image processing experiments.

## Good repo name ideas

- `opencv-image-processing-lab`
- `image-processing-with-opencv`
- `opencv-vision-notebook`
- `digital-image-processing-playground`
